# deploy/kubernetes/rotate-secrets-cron.yaml
apiVersion: batch/v1beta1
kind: CronJob
spec:
  schedule: "0 0 1 * *"  # Primer día de cada mes
  jobTemplate:
    spec:
      containers:
      - name: rotate
        image: vault:latest
        command: ["/security/rotate-secrets.sh"]
